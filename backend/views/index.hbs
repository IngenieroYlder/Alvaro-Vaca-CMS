<!DOCTYPE html>
<html lang="es">

<head>
  {{> head}}
  <title>{{empresaNombre}} - En Construcción</title>
  <meta name="description"
    content="{{empresaNombre}} - {{empresaSlogan}}. Sitio web en construcción. Contáctenos al {{telefono}}.">



  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

  <!-- STATIC LAYOUT STYLES -->
  <style id="static-layout">
    /* GLOBAL RESET */
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    html {
      height: 100%;
    }

    body {
      min-height: 100%;
      overflow-x: hidden;
      display: flex;
      flex-direction: column;
      /* Font handled by inline style fallback */
      font-family: 'Inter', sans-serif;
    }

    .layout-wrapper {
      position: relative;
      width: 100%;
      min-height: 100vh;
      /* Ensure full viewport height */
      display: flex;
      flex-direction: column;
      isolation: isolate;
      /* Create new stacking context */
    }

    .dark-overlay {
      position: absolute;
      inset: 0;
      /* Cover entire parent (top, right, bottom, left) */
      background: rgba(0, 0, 0, 0.4);
      z-index: -1;
      /* Behind content */
      pointer-events: none;
    }

    .content-layer {
      position: relative;
      z-index: 10;
      display: flex;
      flex-direction: column;
      flex: 1;
      /* Grow to fill wrapper */
      width: 100%;
    }

    .app-header {
      padding: 1.5rem 2rem;
      display: flex;
      justify-content: flex-start;
      align-items: center;
      width: 100%;
    }

    .logo {
      font-weight: bold;
      color: white;
      font-size: 1.25rem;
      letter-spacing: -0.025em;
      opacity: 0.9;
    }

    .logo img {
      height: 2.5rem;
      object-fit: contain;
    }

    .container-center {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
      flex: 1;
      padding: 2rem 1rem;
      /* Reduced horizontal padding for mobile */
      width: 100%;
    }

    .hero-title {
      font-size: clamp(2rem, 10vw, 4rem);
      /* Improved responsive scaling */
      font-weight: 800;
      line-height: 1.1;
      margin-bottom: 1.5rem;
      color: white;
      text-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
      max-width: 100%;
    }

    .hero-subtitle {
      font-size: clamp(1rem, 4vw, 1.5rem);
      color: rgba(255, 255, 255, 0.95);
      max-width: 40rem;
      margin: 0 auto;
      font-weight: 400;
      line-height: 1.6;
      padding: 0 1rem;
    }

    .btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      padding: 1rem 2rem;
      border-radius: 2rem;
      font-weight: 600;
      text-decoration: none;
      transition: all 0.3s ease;
      font-size: 1.125rem;
      margin-top: 2.5rem;
      backdrop-filter: blur(5px);
      max-width: 100%;
      white-space: nowrap;
    }

    .btn-whatsapp {
      background-color: rgba(34, 197, 94, 0.9);
      color: white;
      border: 1px solid rgba(255, 255, 255, 0.2);
    }

    .btn-whatsapp:hover {
      transform: translateY(-2px);
      background-color: #22c55e;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
    }

    .badge-construction {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      padding: 0.5rem 1rem;
      border-radius: 9999px;
      background: rgba(255, 255, 255, 0.15);
      border: 1px solid rgba(255, 255, 255, 0.2);
      backdrop-filter: blur(8px);
      margin-bottom: 2.5rem;
      cursor: default;
    }

    .pulse-dot {
      width: 0.5rem;
      height: 0.5rem;
      background-color: #facc15;
      border-radius: 9999px;
      animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
    }

    .badge-construction span:last-child {
      color: white;
      font-size: 0.75rem;
      font-weight: 600;
      letter-spacing: 0.1em;
      text-transform: uppercase;
    }

    .icon {
      width: 1.5rem;
      height: 1.5rem;
      margin-right: 0.5rem;
    }

    @keyframes pulse {

      0%,
      100% {
        opacity: 1;
        transform: scale(1);
      }

      50% {
        opacity: 0.5;
        transform: scale(1.2);
      }
    }

    footer {
      padding: 2rem;
      text-align: center;
      color: rgba(255, 255, 255, 0.6);
      font-size: 0.875rem;
      width: 100%;
      /* Ensure footer takes full width */
    }
  </style>
</head>

<!-- INLINE STYLES -->

<body style="
    --primary: {{{theme.primaryColor}}};
    --secondary: {{{theme.secondaryColor}}};
    --tertiary: {{{theme.tertiaryColor}}};
    font-family: '{{{theme.bodyFont}}}', 'Inter', sans-serif;
    background-color: {{{theme.primaryColor}}};
    background-image: linear-gradient(135deg, {{{theme.primaryColor}}} 0%, {{{theme.tertiaryColor}}} 100%);
    background-attachment: fixed; /* Keep background static during scroll */
    color: white;
  ">

  <div class="layout-wrapper">
    <!-- Overlay for better text contrast -->
    <div class="dark-overlay"></div>

    <!-- Content Layer -->
    <div class="content-layer">

      <!-- Header -->
      <header class="app-header">
        <div class="logo">
          {{#if logoUrl}}
          <img src="{{logoUrl}}" alt="{{empresaNombre}}">
          {{else}}
          <!-- {{empresaNombre}} -->
          {{/if}}
        </div>
      </header>

      <!-- Main Hero -->
      <main class="container-center">

        <!-- Badge -->
        <div class="badge-construction">
          <span class="pulse-dot"></span>
          <span>En Construcción</span>
        </div>

        <!--                    <h2 class="text-5xl md:text-6xl lg:text-7xl font-black tracking-tight text-primary leading-[0.95]">
                        Preguntas Frecuentes
                    </h2>-->
        <!-- Title -->
        <h1 class="hero-title">
          {{empresaNombre}}
        </h1>

        <p class="hero-subtitle">
          {{#if empresaSlogan}}
          {{empresaSlogan}}
          {{else}}
          Transporte y logística de calidad.
          {{/if}}
        </p>

        <!-- CTA -->
        {{#if contactoUrl}}
        <a href="{{contactoUrl}}" target="_blank" rel="noopener noreferrer" class="btn btn-whatsapp">
          <svg class="icon" fill="currentColor" viewBox="0 0 24 24">
            <path
              d="M.057 24l1.687-6.163c-1.041-1.804-1.588-3.849-1.587-5.946.003-6.556 5.338-11.891 11.893-11.891 3.181.001 6.167 1.24 8.413 3.488 2.245 2.248 3.481 5.236 3.48 8.414-.003 6.557-5.338 11.892-11.893 11.892-1.99-.001-3.951-.5-5.688-1.448l-6.305 1.654zm6.597-3.807c1.676.995 3.276 1.591 5.392 1.592 5.448 0 9.886-4.434 9.889-9.885.002-5.462-4.415-9.89-9.881-9.892-5.452 0-9.887 4.434-9.889 9.884-.001 2.225.651 3.891 1.746 5.634l-.999 3.648 3.742-.981zm11.387-5.464c-.074-.124-.272-.198-.57-.347-.297-.149-1.758-.868-2.031-.967-.272-.099-.47-.149-.669.149-.198.297-.768.967-.941 1.165-.173.198-.347.223-.644.074-.297-.149-1.255-.462-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.297-.347.446-.521.151-.172.2-.296.3-.495.099-.198.05-.372-.025-.521-.075-.148-.669-1.611-.916-2.206-.242-.579-.487-.501-.669-.51l-.57-.01c-.198 0-.52.074-.792.372s-1.04 1.016-1.04 2.479 1.065 2.876 1.213 3.074c.149.198 2.095 3.2 5.076 4.487.709.306 1.263.489 1.694.626.712.226 1.36.194 1.872.118.571-.085 1.758-.719 2.006-1.413.248-.695.248-1.29.173-1.414z" />
          </svg>
          Escribir al WhatsApp
        </a>
        {{/if}}

      </main>

      <footer>
        <span>&copy; {{anio}} {{empresaNombre}}.</span> <br>
        {{#if telefono}}
        <span>{{telefono}}</span>
        {{/if}}
        {{#if email}}
        <span style="opacity:0.6; margin-left:8px;">{{email}}</span>
        {{/if}}
      </footer>
    </div>
  </div>
</body>

</html>