{{#> main}}

<div class="bg-gray-50 border-b border-gray-200 pt-32 pb-8">
    <div class="container mx-auto px-4 text-center">
        <h1 class="text-3xl md:text-5xl font-black text-gray-900 mb-2">Registro de Candidatos</h1>
        <p class="text-gray-500 text-lg">Crea tu cuenta para aplicar a nuestras vacantes.</p>
    </div>
</div>

<div class="container mx-auto px-4 py-12">
    <div class="max-w-md mx-auto bg-white rounded-3xl shadow-xl p-8 border border-gray-100">
        <form action="/candidato/registro" method="POST" class="space-y-6">
            {{#if vacanteId}}
            <input type="hidden" name="vacanteId" value="{{vacanteId}}">
            <div class="bg-blue-50 text-blue-700 p-4 rounded-xl text-sm mb-4">
                Estás aplicando a: <strong>{{vacanteTitulo}}</strong>. Regístrate para continuar.
            </div>
            {{/if}}

            <div class="space-y-2">
                <label class="font-bold text-gray-700">Nombre Completo</label>
                <input type="text" name="nombre" required
                    class="w-full p-3 rounded-xl border border-gray-200 focus:border-secondary focus:ring-4 focus:ring-secondary/10 outline-none transition-all">
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                <div class="space-y-2">
                    <label class="font-bold text-gray-700">Celular de Contacto <span
                            class="text-red-500">*</span></label>
                    <input type="tel" name="telefono" required placeholder="Ej: 3001234567"
                        class="w-full p-3 rounded-xl border border-gray-200 focus:border-secondary focus:ring-4 focus:ring-secondary/10 outline-none transition-all">
                </div>
                <div class="space-y-2">
                    <label class="font-bold text-gray-700">WhatsApp <span class="text-red-500">*</span></label>
                    <input type="tel" name="whatsapp" required placeholder="Ej: 3001234567"
                        class="w-full p-3 rounded-xl border border-gray-200 focus:border-secondary focus:ring-4 focus:ring-secondary/10 outline-none transition-all">
                    <p class="text-xs text-gray-400">Puede ser el mismo número.</p>
                </div>
            </div>

            <div class="space-y-2">
                <label class="font-bold text-gray-700">Correo Electrónico</label>
                <input type="email" name="email" required
                    class="w-full p-3 rounded-xl border border-gray-200 focus:border-secondary focus:ring-4 focus:ring-secondary/10 outline-none transition-all">
            </div>

            <div class="space-y-2">
                <label class="font-bold text-gray-700">Contraseña</label>
                <input type="password" name="password" required
                    class="w-full p-3 rounded-xl border border-gray-200 focus:border-secondary focus:ring-4 focus:ring-secondary/10 outline-none transition-all">
            </div>

            <button type="submit"
                class="w-full py-4 bg-secondary text-white font-bold rounded-xl hover:bg-secondary/90 shadow-lg shadow-secondary/30 transition-all hover:-translate-y-1">
                Registrarme
            </button>

            <p class="text-center text-gray-500 text-sm mt-6">
                ¿Ya tienes cuenta? <a href="/login-candidato{{#if vacanteId}}?vacante={{vacanteId}}{{/if}}"
                    class="text-secondary font-bold hover:underline">Inicia Sesión</a>
            </p>
        </form>
    </div>
</div>

{{/main}}