<!DOCTYPE html>
<html lang="es">

<head>
    <title>{{title}}</title>
    {{> head}}
    <!-- Material Symbols (Already in head but redundant ok, leaving relevant only) -->
</head>

<body
    class="bg-gray-50 font-sans text-gray-800 antialiased selection:bg-primary/20 selection:text-primary flex flex-col min-h-screen">
    {{> header}}

    <main class="flex-grow pt-32 pb-16 px-4 sm:px-6 lg:px-8">
        <article class="max-w-4xl mx-auto bg-white rounded-2xl shadow-sm border border-gray-100 p-8 md:p-12">
            <header class="mb-10 text-center">
                <h1 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">{{pagina.titulo}}</h1>
                <div class="h-1 w-20 bg-secondary mx-auto rounded-full"></div>
            </header>

            <div
                class="prose prose-lg max-w-none prose-headings:text-primary prose-a:text-secondary hover:prose-a:text-primary transition-colors">
                {{#if pagina.contenido}}
                {{{pagina.contenido}}}
                {{else}}
                <div class="bg-yellow-50 border-l-4 border-yellow-400 p-4 rounded-r-lg">
                    <div class="flex">
                        <div class="flex-shrink-0">
                            <span class="material-symbols-outlined text-yellow-500">info</span>
                        </div>
                        <div class="ml-3">
                            <p class="text-sm text-yellow-700">
                                El contenido de esta página está en construcción. Por favor, edítalo desde el panel de
                                administración.
                            </p>
                        </div>
                    </div>
                </div>
                {{/if}}
            </div>

            <div class="mt-12 pt-8 border-t border-gray-100 text-center text-sm text-gray-500">
                <p>Última actualización: {{formatDate pagina.fechaActualizacion}}</p>
            </div>
        </article>
    </main>

    {{> footer}}
</body>

</html>