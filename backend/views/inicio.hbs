<!DOCTYPE html>
<html lang="es" class="scroll-smooth">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{#if meta.browserTitle}}{{meta.browserTitle}}{{else}}Alvaro Vaca - Candidato al Senado 2026{{/if}}</title>
    <meta name="description"
        content="{{#if meta.browserDescription}}{{meta.browserDescription}}{{else}}Elecciones Congreso, Marzo 8. Sitio oficial de Alvaro Vaca, candidato al Senado 2026.{{/if}}">

    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700;800;900&family=Open+Sans:wght@400;600&display=swap"
        rel="stylesheet">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons+Round" rel="stylesheet">
    <link
        href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200"
        rel="stylesheet">

    <!-- Tailwind CSS (via CDN for simplicity as per design) -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    colors: {
                        primary: "#006838",     // Official Dark Green
                        secondary: "#0D9A49",   // Medium Green
                        sunflower: "#D7DF23",   // Yellow-Green
                        accent: "#F89621",      // Orange
                        "background-light": "#F9FAFB",
                        "background-dark": "#111827",
                    },
                    fontFamily: {
                        display: ["Montserrat", "sans-serif"],
                        body: ["Open Sans", "sans-serif"],
                    },
                },
            },
        }
    </script>
    <style>
        .text-gradient {
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-image: linear-gradient(to right, #006838, #0D9A49);
        }

        .pattern-grid {
            background-image: radial-gradient(#0D9A49 1px, transparent 1px);
            background-size: 20px 20px;
            opacity: 0.1;
        }
    </style>
</head>

<body class="bg-background-light text-gray-800 font-body antialiased">

    <!-- Navigation -->
    {{> header}}

    <!-- Hero Section -->
    <section id="inicio"
        class="relative pt-32 pb-20 lg:pt-48 lg:pb-32 overflow-hidden bg-gradient-to-br from-gray-50 to-gray-100">
        <!-- Background Elements -->
        <div class="absolute inset-0 z-0 pointer-events-none">
            <div class="absolute inset-0 pattern-grid"></div>
            <div
                class="absolute top-0 right-0 w-1/2 h-1/2 bg-sunflower/20 rounded-full blur-3xl translate-x-1/3 -translate-y-1/3">
            </div>
            <div
                class="absolute bottom-0 left-0 w-1/2 h-1/2 bg-secondary/10 rounded-full blur-3xl -translate-x-1/3 translate-y-1/3">
            </div>
        </div>

        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
            <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">

                <!-- Text Content -->
                <div class="text-center lg:text-left space-y-8">
                    <div
                        class="inline-flex items-center px-4 py-2 rounded-full bg-white border border-gray-200 shadow-sm text-primary font-semibold text-sm">
                        <span class="w-2 h-2 rounded-full bg-sunflower mr-2 animate-pulse"></span>
                        {{#if meta.hero.badge}}{{meta.hero.badge}}{{else}}Elecciones Congreso 2026, Marzo 8{{/if}}
                    </div>

                    <h1 class="font-display font-black text-5xl lg:text-7xl leading-tight text-gray-900">
                        {{#if meta.hero.title}}{{{meta.hero.title}}}{{else}}Un Senador que <br><span
                            class="text-gradient">Conecte con la Gente</span>{{/if}}
                    </h1>

                    <!-- Mobile Image (Visible only on mobile) -->
                    <div class="relative h-[400px] flex justify-center items-end lg:hidden w-full my-8">
                        <div
                            class="absolute inset-x-0 bottom-0 h-4/5 bg-gradient-to-t from-primary/10 to-transparent rounded-[2rem] transform rotate-3">
                        </div>
                        <div
                            class="absolute inset-x-0 bottom-0 h-4/5 bg-sunflower/20 rounded-[2rem] transform -rotate-3">
                        </div>

                        <img src="{{#if meta.hero.mobileImage}}{{meta.hero.mobileImage}}{{else}}/assets/FOTO CAMPAÑA.png{{/if}}"
                            alt="Candidato Alvaro Vaca" class="relative z-10 max-h-full object-contain drop-shadow-2xl">

                        <!-- Floating Badge -->
                        <div class="absolute bottom-4 right-4 z-20 bg-white p-3 rounded-xl shadow-xl border border-gray-100 animate-bounce"
                            style="animation-duration: 3s;">
                            <img src="/assets/5_LOGO.png" alt="Vota #75" class="w-16 h-auto">
                        </div>
                    </div>

                    <p class="text-lg lg:text-xl text-gray-600 max-w-2xl mx-auto lg:mx-0">
                        {{#if meta.hero.description}}{{meta.hero.description}}{{else}}Soy Álvaro Vaca, un candidato
                        nuevo, que no hace parte de la politiquería tradicional...{{/if}}
                    </p>

                    <div class="flex flex-col sm:flex-row items-center justify-center lg:justify-start gap-4">
                        <a href="{{#if meta.hero.ctaPrimary.url}}{{meta.hero.ctaPrimary.url}}{{else}}#propuestas{{/if}}"
                            class="w-full sm:w-auto px-8 py-4 bg-primary hover:bg-green-800 text-white font-bold rounded-xl shadow-lg shadow-green-900/20 transform hover:-translate-y-0.5 transition-all text-center">
                            {{#if meta.hero.ctaPrimary.text}}{{meta.hero.ctaPrimary.text}}{{else}}Conoce mis
                            Propuestas{{/if}}
                        </a>
                        <a href="{{#if meta.hero.ctaSecondary.url}}{{meta.hero.ctaSecondary.url}}{{else}}#biografia{{/if}}"
                            class="w-full sm:w-auto px-8 py-4 bg-white hover:bg-gray-50 text-primary font-bold rounded-xl border border-gray-200 shadow-md transform hover:-translate-y-1 transition-all flex items-center justify-center">
                            <span class="material-icons-round mr-2">play_circle</span>
                            {{#if meta.hero.ctaSecondary.text}}{{meta.hero.ctaSecondary.text}}{{else}}Ver
                            Historia{{/if}}
                        </a>
                    </div>

                    <div class="pt-8 border-t border-gray-200">
                        <p class="text-sm text-gray-500 font-semibold mb-3">APOYADO POR</p>
                        <div
                            class="flex items-center justify-center lg:justify-start gap-6 opacity-80 hover:opacity-100 transition-all duration-500">
                            <img src="/assets/5_LOGO.png" alt="Partido Alianza Verde #75"
                                class="h-24 md:h-32 w-auto object-contain">
                        </div>
                    </div>
                </div>

                <!-- Image -->
                <div class="relative lg:h-[600px] hidden lg:flex justify-center items-end">
                    <div
                        class="absolute inset-x-0 bottom-0 h-4/5 bg-gradient-to-t from-primary/10 to-transparent rounded-[3rem] transform rotate-3">
                    </div>
                    <div class="absolute inset-x-0 bottom-0 h-4/5 bg-sunflower/20 rounded-[3rem] transform -rotate-3">
                    </div>

                    <img src="{{#if meta.hero.image}}{{meta.hero.image}}{{else}}/assets/FOTO CAMPAÑA.png{{/if}}"
                        alt="Candidato Alvaro Vaca"
                        class="relative z-10 max-h-full object-contain drop-shadow-2xl hover:scale-105 transition-transform duration-700">

                    <!-- Floating Badge -->
                    <div class="absolute bottom-10 right-10 z-20 bg-white p-4 rounded-2xl shadow-xl border border-gray-100 animate-bounce"
                        style="animation-duration: 3s;">
                        <img src="/assets/5_LOGO.png" alt="Vota #75" class="w-24 h-auto">
                    </div>
                </div>

            </div>
        </div>
    </section>

    <!-- Bio Section -->
    <section id="biografia" class="py-12 bg-white">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="grid grid-cols-1 lg:grid-cols-2 gap-16 items-start">
                <div class="relative hidden lg:block min-h-[350px] aspect-[4/5]">
                    <div
                        class="absolute top-0 left-0 w-20 h-20 bg-sunflower rounded-full opacity-20 -translate-x-1/2 -translate-y-1/2">
                    </div>
                    <div
                        class="rounded-3xl overflow-hidden shadow-2xl bg-gradient-to-br from-primary via-secondary to-sunflower relative group h-full">
                        <img src="{{#if meta.bio.image}}{{meta.bio.image}}{{else}}/assets/alvaro.png{{/if}}"
                            class="w-full h-full object-cover opacity-90 min-h-[350px]" style="object-position: top;">
                    </div>
                </div>
                <div class="space-y-6 lg:sticky lg:top-32 self-start">
                    <h2 class="font-display font-bold text-4xl text-gray-900">{{#if
                        meta.bio.title}}{{meta.bio.title}}{{else}}Una Vida de Servicio{{/if}}</h2>
                    <div class="w-20 h-1.5 bg-gradient-to-r from-primary to-sunflower rounded-full"></div>

                    <!-- Mobile Image -->
                    <div
                        class="relative lg:hidden w-full md:w-3/5 md:mx-auto h-64 md:h-[480px] rounded-2xl overflow-hidden shadow-xl bg-gradient-to-br from-primary via-secondary to-sunflower my-6">
                        <img src="{{#if meta.bio.image}}{{meta.bio.image}}{{else}}/assets/alvaro.png{{/if}}"
                            class="w-full h-full object-cover opacity-90 object-top">
                    </div>

                    <p class="text-lg text-gray-600 leading-relaxed">
                        {{#if meta.bio.description}}{{meta.bio.description}}{{else}}Reservista de la Fuerza Aérea
                        Colombiana...{{/if}}
                    </p>

                    <div class="pt-4">
                        <a href="{{#if meta.bio.linkUrl}}{{meta.bio.linkUrl}}{{else}}/biografia{{/if}}"
                            class="inline-flex items-center px-6 py-3 bg-primary text-white font-bold rounded-xl shadow-lg hover:bg-green-800 transition-all transform hover:-translate-y-0.5">
                            {{#if meta.bio.linkText}}{{meta.bio.linkText}}{{else}}Leer Biografía Completa{{/if}}
                            <span class="material-icons-round ml-2">arrow_forward</span>
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Proposals Section -->
    <section id="propuestas" class="py-20 bg-gray-50">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="text-center max-w-3xl mx-auto mb-16">
                <h2 class="font-display font-bold text-4xl text-gray-900 mb-4">Mis Propuestas</h2>
                <p class="text-xl text-gray-600">
                    Ejes fundamentales para transformar a Colombia desde el Senado.
                </p>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                {{#if meta.propuestas}}
                {{#each meta.propuestas}}
                <div
                    class="bg-white rounded-2xl p-8 shadow-lg hover:shadow-xl transition-shadow border-t-4 border-{{this.color}} group">
                    <div
                        class="w-16 h-16 rounded-2xl bg-gradient-to-br from-gray-100 to-white flex items-center justify-center mb-6 group-hover:scale-110 transition-transform duration-300 shadow-sm">
                        <span class="material-icons-round text-4xl text-{{this.color}}-600">{{this.icon}}</span>
                    </div>
                    <h3 class="font-display font-bold text-xl text-gray-900 mb-3">{{this.title}}</h3>
                    <p class="text-gray-600">{{this.description}}</p>
                </div>
                {{/each}}
                {{else}}
                <!-- Fallback Proposals -->
                <!-- Fallback Proposals -->
                <div
                    class="bg-white rounded-2xl p-8 shadow-lg hover:shadow-xl transition-shadow border-t-4 border-primary group">
                    <div
                        class="w-16 h-16 rounded-2xl bg-gradient-to-br from-green-100 to-green-50 flex items-center justify-center mb-6 group-hover:scale-110 transition-transform duration-300 shadow-sm">
                        <span class="material-icons-round text-4xl text-primary bg-clip-text">lightbulb</span>
                    </div>
                    <h3 class="font-display font-bold text-xl text-gray-900 mb-3">Educación para emprender</h3>
                    <p class="text-gray-600">Desde la primaria enseñamos a los nuevos emprendedores.</p>
                </div>

                <div
                    class="bg-white rounded-2xl p-8 shadow-lg hover:shadow-xl transition-shadow border-t-4 border-sunflower group">
                    <div
                        class="w-16 h-16 rounded-2xl bg-gradient-to-br from-yellow-100 to-yellow-50 flex items-center justify-center mb-6 group-hover:scale-110 transition-transform duration-300 shadow-sm">
                        <span class="material-icons-round text-4xl text-yellow-600">balance</span>
                    </div>
                    <h3 class="font-display font-bold text-xl text-gray-900 mb-3">Cero impunidad</h3>
                    <p class="text-gray-600">El hacinamiento en las cárceles, es la principal causa de la impunidad en
                        Colombia</p>
                </div>

                <div
                    class="bg-white rounded-2xl p-8 shadow-lg hover:shadow-xl transition-shadow border-t-4 border-accent group">
                    <div
                        class="w-16 h-16 rounded-2xl bg-gradient-to-br from-red-100 to-red-50 flex items-center justify-center mb-6 group-hover:scale-110 transition-transform duration-300 shadow-sm">
                        <span class="material-icons-round text-4xl text-accent">security</span>
                    </div>
                    <h3 class="font-display font-bold text-xl text-gray-900 mb-3">Ley de Seguridad Digital e IA</h3>
                    <p class="text-gray-600">Las redes sociales y la IA (Inteligencia Artificial), requieren una
                        regulación estricta.</p>
                </div>
                {{/if}}
            </div>

            <div class="mt-16 text-center">
                <a href="/propuestas"
                    class="inline-flex items-center px-6 py-3 bg-primary text-white font-bold rounded-xl shadow-lg hover:bg-green-800 transition-all transform hover:-translate-y-0.5">
                    Conocer más detalles
                    <span class="material-icons-round ml-2">arrow_forward</span>
                </a>
            </div>
        </div>
    </section>

    <!-- Join Modal -->
    {{> join_modal}}

    <!-- Scripts -->
    <script>
        function openJoinModal() {
            const modal = document.getElementById('joinModal');
            if (modal) modal.classList.remove('hidden');
        }
        function closeJoinModal() {
            const modal = document.getElementById('joinModal');
            if (modal) modal.classList.add('hidden');
        }
        // Expose to global scope for other scripts
        window.openJoinModal = openJoinModal;
        window.closeJoinModal = closeJoinModal;
    </script>

    <!-- Footer -->
    {{> footer}}

</body>

</html>